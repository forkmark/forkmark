fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
httpx>=0.27.0
tenacity>=8.4.0


# JWT verification (used by auth_middleware for Bearer token auth)
PyJWT>=2.8.0

# API key hashing — argon2id with embedded salt (replaces bare SHA-256)
argon2-cffi>=23.1.0

# PostgreSQL support (optional — only needed when FM_DATABASE_URL is set)
psycopg2-binary>=2.9.9

# DuckDB columnar OLAP (optional — only needed when FM_TRACE_BACKEND=duckdb)
duckdb>=1.0.0

# ── Optional divergence scorers ────────────────────────────────────────────────
# Semantic scoring (FM_DIVERGENCE_SCORER=semantic or auto):
#   pip install "forkmark[semantic]"  OR  pip install sentence-transformers numpy
# sentence-transformers>=2.7.0
# numpy>=1.26.0

# LLM-as-judge and OpenAI embedding scorers use httpx (already installed above)
# and only need FM_OPENAI_API_KEY to be set — no extra packages required.

# ── Optional OpenTelemetry tracing ──────────────────────────────────────────
# pip install opentelemetry-api opentelemetry-sdk
# opentelemetry-api>=1.20.0
# opentelemetry-sdk>=1.20.0

# ── Optional JSON schema validation for auto-evaluators ─────────────────────
# pip install jsonschema
# jsonschema>=4.20.0
regex==2024.5.15
redis==5.0.4
celery==5.4.0
